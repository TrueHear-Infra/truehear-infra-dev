apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# gcp-vars.yaml carries the non-secret GCP project identifiers used by
# postBuild substitution (issue #72).
resources:
  - gcp-vars.yaml
