apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# namespace.yaml pre-creates cnrm-system so the WIF credential Secret has a
# home before the operator runs; KCC adopts the existing namespace.
resources:
  - namespace.yaml
  - kcc-wif-credentials.yaml
  - configconnector.yaml
